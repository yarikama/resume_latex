% Relational DB Engine -- Notion variant of projects/relational-db-engine.tex,
% compressed from two bullets to one. Notion needs "familiarity with backend systems,
% from the user interface to the data model", which this covers as depth signal; it is
% not the centrepiece here (Ricebook is), so it pays for the page budget.
% Same wording caveat as the original: the course supplies scaffolding, "built" is
% standard framing, be ready to say so in an interview. No public repo by course policy.
  \resumeProjectEntry
    {\textbf{Relational Database Engine from Scratch} $|$ \emph{C++, B+ Tree, Query Optimization, TPC-H}}{Aug. 2025 -- Dec. 2025}
    \resumeItemListStart
      \resumeItem{\textbf{Storage \& Query Engine:} Built the C++ persistence layer, a buffer manager with \textbf{LRU} eviction, a \textbf{B+ tree} index, and a \textbf{sort-merge join} over external merge sort, under a SQL compiler and cost-based optimizer; ran \textbf{TPC-H} at scale factor 1}
    \resumeItemListEnd%