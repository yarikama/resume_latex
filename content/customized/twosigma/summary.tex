% SUMMARY -- Two Sigma "Software Engineering Full-Time Campus Hire (Houston)" (req 14018).
% The stock SWE summary leads with the B2B product story (users, enterprise logos), which
% is the wrong currency for a quant firm. This JD's responsibilities are: distributed
% storage, reliability of mission-critical systems, "environments that test and deploy our
% quantitative strategies", low-latency execution infra, and "infrastructure used to ingest
% data from 10,000+ sources daily". So this version leads with pipeline scale and test/deploy
% infrastructure, and names Java explicitly (the JD's first-listed language; the Google test
% infra was Java + Python). Every number traces to cv.md.
\item\small{\textbf{Backend and infrastructure engineer who scales data pipelines and the test infrastructure that keeps them correct. Scaled an async indexing pipeline from \textbf{3M to 20M+} text chunks (\textbf{3.5x} faster parsing), optimized \textbf{140+} APIs (\textbf{27.7\%} faster), and cut production hotfixes by \textbf{90\%}. At \textbf{Google}, built \textbf{3} integration test infrastructures (\textbf{Java}, \textbf{Python}) from zero across \textbf{400+} test suites. M.C.S. at Rice, Dec. 2026.}}%
