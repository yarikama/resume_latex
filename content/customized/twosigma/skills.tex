% SKILLS -- Two Sigma variant (req 14018).
% Reordered so the JD's named languages land first and in its own order: "Java, C, C++,
% Python, Ruby, Perl, or other languages that target the JVM". A Systems group is added,
% which the stock SWE list omits entirely -- every item in it is evidenced by the
% Relational DB Engine (COMP 530) or Chat Bar entries on this page, and by cv.md.
% React and the whole frontend group are dropped: this JD has no frontend component, and
% the width buys the Systems group.
%
% WIDTH RULE: each \item must render as exactly ONE printed line or the block pushes the
% resume to 2 pages -- this is the only thing that broke the one-page fit while building
% this variant. Dropped purely for width, not accuracy (all still in cv.md): TCP Sockets,
% MongoDB, Neo4j, Shell Script, ElastiCache.
\item\small{\textbf{Languages:} Java, C/C++, Python, TypeScript $|$ \textbf{Systems:} Distributed Storage, B+ Tree Indexing, Query Optimization, Multi-threading}
\item\small{\textbf{Backend:} Django, FastAPI, Express.js $|$ \textbf{APIs:} RESTful, WebSocket, Redis Pub/Sub $|$ \textbf{Async:} asyncio, Celery $|$ \textbf{Testing:} pytest, JUnit}
\item\small{\textbf{Databases:} PostgreSQL, MySQL, Elasticsearch, Milvus $|$ \textbf{DevOps:} GitHub Actions, Docker, git, Nginx $|$ \textbf{AWS:} EC2, S3, RDS}%
