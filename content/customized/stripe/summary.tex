% SUMMARY -- Stripe "Software Engineer, New Grad" (req 8128744).
% Two things this JD rewards that the stock SWE summary buries:
%   1. Stripe's self-description is "we maintain some of the most reliable APIs on the
%      internet" -- so the 140+ API / 27.7% latency number is the headline, not a mid-bullet.
%   2. "Ability to leverage AI tools to accelerate development while applying rigorous
%      critical thinking" is a MINIMUM requirement, not a nice-to-have. The Google LLM
%      triage agent is direct evidence. It did NOT survive the one-page fit here, but it
%      is still carried twice on the page: the Google "Automated Failure Triage" bullet
%      and the AI-Assisted Dev group in the skills block.
% Java is named first because Stripe's stack line is "Java, Ruby, JavaScript, Scala, Go"
% (Python is absent from it). Every number traces to cv.md.
\item\small{\textbf{Backend engineer focused on API reliability and the tooling that keeps it honest. Optimized \textbf{140+} RESTful APIs (\textbf{27.7\%} faster), scaled an async pipeline from 3M to \textbf{20M+} text chunks, and cut production hotfixes by \textbf{90\%}. At \textbf{Google}, built \textbf{3} integration test infrastructures (\textbf{Java}, \textbf{Python}) from a zero baseline. M.C.S. at Rice, Dec. 2026.}}%
